services:
  postgres:
    image: postgres:17-alpine
    container_name: sidwell-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-sidwell}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      # Config sanatoasa de dev; NUMERIC-ul si timestamptz-ul nu depind de asta, dar tine locale predictibil.
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - sidwell-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-sidwell}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  sidwell-pgdata:
    name: sidwell-pgdata
